async def process_metadata(directory: str, save_path: str = None):
    """
    Process all metadata files in a directory.

    Args:
        directory (str): The directory containing metadata files.
        save_path (str): The directory to save the processed metadata files.
    """
    ...
